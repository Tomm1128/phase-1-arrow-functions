// First function should be a regular function to divide two numbers.

function divide (num1, num2){
        return num1 / num2;
}

// Here is a arrow function of the function above
// const divide = (num1, num2) => num1 / num2;

// Arrow function to square a number
const square = num1 => num1 * num1;

// Arrow function to add two numbers
const add = (num1, num2) => num1 + num2;
